import { FC } from "react";
import { useTranslation } from "react-i18next";
import AdminLogin from "./AdminLogin";

const AdminPage: FC = () => {
  const { t } = useTranslation();

  return <div className="">pageeeee</div>;
};

export default AdminPage;
